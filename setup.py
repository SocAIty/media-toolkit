from setuptools import setup, find_packages

setup(
    name='multimodal-files',
    version='0.0.0',
    description="",
    author='SocAIty',
    packages=find_packages(),
    install_requires=[
        'multimodal-files'
        'opencv-python',
        'numpy'
    ]
)